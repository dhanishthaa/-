# Landing restoration notes

The live `/` route mounts the Landing component and shows the full HELLO typewriter, isth logo, two-line quote, and Explore the Collection CTA at scroll position zero. The current Home navbar markup is explicitly `Collection`, `About`, `Contact`; the root route is separate from `/home`.
