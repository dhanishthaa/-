# Verification notes

The `/about` route now renders at the top of the page: the header, masthead, and editorial feature image appear before the Philosophy section. The full-page view confirms the page is not restoring the previous mid-page scroll position.

The feature image contains the requested “ESTABLISHED 2021 · GUJARAT, INDIA” metadata rendered in a single Cherry-red treatment; the stylesheet now removes text shadow, background, and text-fill mixing from the live rule. The mobile landing edge, desktop-timed signature completion, and theme-color changes remain present in `Landing.tsx` and `index.css`.
