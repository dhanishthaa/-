# Nakshatra badge verification

The Coming soon — isth badge was checked in the browser after the CSS change. Its computed border radius is `0px`, while its translucent white background and `blur(22px) saturate(1.25)` frost effect remain active.
