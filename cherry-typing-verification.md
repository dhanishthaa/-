# Cherry signature verification

The root landing was verified in both required states. During typing, the page visibly uses the exact `#5B0D18` cherry surface with fine grain and no grey patch behind the partial white signature. After the signature completes, the supplied floral-blur background returns with the full white mark, quote, and lower CTA; no rectangular signature artifact is present.
