# Signature opening validation

The landing now has no HELLO element. On a fresh load, the screen begins as a blank ink field and the first visible mark is the `i` segment of the supplied isth signature. After the four sequential reveals complete, the large full signature appears before the flower-blur atmosphere, quote, and Explore the Collection CTA become visible.

The completed state remains on `/` with the full large white isth signature centered over the flower-blur field; the quote and CTA appear only after the signature sequence completes. The root route does not auto-navigate to the home page without scroll or CTA action.
